"""Top-level ai package.

This file makes `ai` an importable package for tests and runtime. It is intentionally
minimal so the codebase can be used both as a package and as a namespace during tests.
"""
# Package marker - keep minimal to avoid side effects during import
__all__ = []
